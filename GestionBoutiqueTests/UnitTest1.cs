using Microsoft.VisualStudio.TestTools.UnitTesting;
using System;
using System.Windows.Forms;

namespace BelbachaBoutique
{
    [TestClass]
    public class UnitTest1
    {
        [TestMethod]
        public void TestGestionFrmClient()
        {
            // Arrange
            //DataGridView dgvClient = new DataGridView();
            //// Supposez que vous avez également une instance de GestionClients utilisée dans votre application.

            //// Act
            //Form frmDemmarage = new FrmDemmarage();
            //frmDemmarage
            //.GestionFrmClient(); // Remplacez "VotreClasse" par le nom de votre classe contenant la méthode à tester

            //// Assert
            //// Vérifiez si la source de données de dgvClient a été correctement mise à jour
            //Assert.IsNotNull(dgvClient.DataSource, "La source de données du DataGridView n'a pas été correctement mise à jour.");
            //Assert.AreEqual(GestionClients.getTuples().Rows.Count, ((DataTable)dgvClient.DataSource).Rows.Count, "Le nombre de lignes dans la source de données du DataGridView ne correspond pas au nombre de lignes renvoyées par GestionClients.getTuples().");
            // Vous pouvez ajouter d'autres assertions pour vérifier d'autres aspects de la mise à jour de la source de données si nécessaire.
        }
    }
}
